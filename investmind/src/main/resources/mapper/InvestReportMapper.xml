<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.investmind.repository.InvestReportMapper">

    <!-- 分页查询报告列表 -->
    <select id="selectReportPage" resultType="com.investmind.model.InvestReport">
        SELECT *
        FROM invest_report
        WHERE deleted = 0
        AND user_id = #{userId}
        <if test="reportType != null and reportType != ''">
            AND report_type = #{reportType}
        </if>
        <if test="startDate != null">
            AND report_date &gt;= #{startDate}
        </if>
        <if test="endDate != null">
            AND report_date &lt;= #{endDate}
        </if>
        ORDER BY report_date DESC, created_time DESC
    </select>

</mapper>
